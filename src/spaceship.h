#ifndef SPACESHIP_H
#define SPACESHIP_H


class SpaceShip {
public:
    SpaceShip() {}
    bool alive{true};
    float position_x;
    float position_y;
    float angle;
    float side_length = 20; // length in pixels

private:

};
#endif